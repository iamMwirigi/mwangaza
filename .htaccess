<IfModule mod_rewrite.c>
    RewriteEngine On
    # If the request doesn't already start with /public, redirect it there
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
